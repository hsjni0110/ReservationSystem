rootProject.name = 'ReservationSystem'

include(
        "core-api",
        "payment",
        "common",
        "account",
        "user",
        "reservation",
        "event-outbox"
)

project(':core-api').projectDir = file('core/core-api')